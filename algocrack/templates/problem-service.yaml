apiVersion: apps/v1
kind: Deployment
metadata:
  name: problem-service
  namespace: {{ include "algocrack.namespace" . }}
  labels:
    {{- include "algocrack.labels" . | nindent 4 }}
    {{- include "algocrack.componentLabel" "problem-service" | nindent 4 }}
spec:
  replicas: {{ .Values.services.problemService.replicaCount }}
  selector:
    matchLabels:
      {{- include "algocrack.componentSelectorLabels" (dict "context" . "component" "problem-service") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "algocrack.componentSelectorLabels" (dict "context" . "component" "problem-service") | nindent 8 }}
    spec:
      {{- include "algocrack.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: problem-service
          image: {{ printf "%s:%s" .Values.images.problemService.repository .Values.images.problemService.tag }}
          imagePullPolicy: {{ .Values.images.problemService.pullPolicy }}
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: algocrack-config
            - secretRef:
                name: {{ include "algocrack.appSecretName" . }}
          env:
            - name: SERVER_PORT
              value: "8084"
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: algocrack-config
                  key: PROBLEM_DATASOURCE_URL
          resources:
            {{- toYaml .Values.services.problemService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 40
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: problem-service
  namespace: {{ include "algocrack.namespace" . }}
  labels:
    {{- include "algocrack.labels" . | nindent 4 }}
    {{- include "algocrack.componentLabel" "problem-service" | nindent 4 }}
spec:
  selector:
    {{- include "algocrack.componentSelectorLabels" (dict "context" . "component" "problem-service") | nindent 4 }}
  ports:
    - name: http
      port: 8084
      targetPort: 8084
